{% extends '::movil.html.twig' %}
{% block header %}
<h1>Crear Cliente</h1>
<a href="{{ path('cliente') }}" data-role="button" data-icon="back" class="ui-btn-right" data-iconpos="notext"></a>
{% endblock %}
{% block content %}
<form action="{{ path('cliente_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Crear</button>
    </p>
</form>
{% endblock %}
